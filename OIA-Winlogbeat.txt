# Winlogbeat config file for org

# Variables you will need to complete for your winlogbeat installation
ls_index = vt_outreach											# Change to your index value
HostName_append = oia.ad.vt.edu									# String to append to the end of your hostnames, OIA uses FQDN
systemClass = general use										# Another departmental choice value to be included in system tags
beatVersion = 8.17.4												# The version of Winlogbeat supported by Virginia Tech

# Variables that may need to change depending on VT changes to the CLS Project
winlogbeatPath = C:\Program Files\winlogbeat					# path winlogbeat will be installed to
execPath =	winlogbeatPath\winlogbeat.exe						# file path to the winlogbeat executable
configPath = $winlogbeatPath\winlogbeat.yml						# file path to the configuration file

# Variables to specify download URLs
winlogbeatZipURL = https://artifacts.elastic.co/downloads/beats/winlogbeat/winlogbeat-$beatVersion-windows-x86_64.zip				# Winlogbeat download URL
winlogbeatSHAurl = https://artifacts.elastic.co/downloads/beats/winlogbeat/winlogbeat-$beatVersion-windows-x86_64.zip.sha512		# Winlogbeat SHA 512 download URL
certChainURL = https://www.pki.vt.edu/incommon_tls_chain_new.pem																	# Virginia Tech certificate chain download URL
